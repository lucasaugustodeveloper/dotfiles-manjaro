# Analise de componente

Alterar o **selectedStyle** para um **hashMap de modificadores**, para ter um maior controle dos estilos.

Também precisa alterar o segundo span, pois ele está fora do padrão do figma.

Fazia a separação do componentes de faz o render do componente do componentes que faz a lógica para inserir estilos.

Com a separação entre apresentação e lógica de componentes, criar props com nomes legíveis para um melhor leitura dos componentes.

Falta validação para inserir estilo quando um step já foi concluído.

Fazer validação para excluir steps que ainda não foram completo.

Não está claro no componente ou definição dele, ser ele vai ser clicável ou não.

Inserir semântica HTMl para uma melhor accessibilidade.

Implemente um sistema de ícones para uma melhor apresentação visual e deixar ele mas personalizado.

evitar utilizar validação do path exclusivamente por string, pois pode quebrar caso os nomes não tenham a mesma correspôndencia.

Precisa de validação para caso haja um longo caminho no breadcrumb, seria pois validar e encurtar casoo tenha muitos steps.

Inserir suporte a ícones.

Seria bom adicionar métodos para testar o componente, atualmente não está validado quando um item está ativo.
